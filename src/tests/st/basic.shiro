//! run

fn f(i: i32, f: i32) -> i32 {
	return (f + 2) * i;
}

fn main() -> i32 {
	var i: i32;

	i = 3;
	if (i > 5) {
		i = 10;
	}
	else {
		i = 20;
	}

	while (i > 10) {
		i = 10;
	}

	f(10, i);
	return 0;
}
